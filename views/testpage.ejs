<!-- index.html -->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Color Scheme Example</title>
    <style>
      :root {
          --color-primary: var(--color-primary, #3498db);
          --color-secondary: var(--color-secondary, #2ecc71);
          --color-accent: var(--color-accent, #e74c3c);
          --color-background: var(--color-background, #ecf0f1);
          --color-text: var(--color-text, #34495e);
          /* Add more CSS variables as needed */
      }

      body {
          background-color: var(--color-background);
          color: var(--color-text);
      }

    </style>
</head>
<body>
    <div id="app">
        <!-- Your application content goes here -->
        <h1>Hello, Color Scheme!</h1>
        <!-- Button to change color scheme -->
        <button id="changeColorScheme">Change Color Scheme</button>
    </div>
    <script type="module" src="themeswitcher.js"></script>
</body>
</html>
