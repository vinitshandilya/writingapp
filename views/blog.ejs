<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap JS (optional, if you need JavaScript features) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <link href="testpage.css" rel="stylesheet">
    <title><%= blog.title %></title>
</head>
<body>
    <h1><%= blog.title %></h1>

    <!-- Toggle between content and edit form -->
    <% if (editing) { %>
        <!-- Edit Form -->
        <form action="/index/blogs/<%= blog._id %>/edit" method="post">
            <label for="editTitle">Title:</label>
            <input type="text" id="editTitle" name="editTitle" value="<%= blog.title %>" required>

            <label for="editBody">Body:</label>
            <textarea id="editBody" name="editBody" rows="4" required><%= blog.body %></textarea>

            <button type="submit">Save Changes</button>
        </form>
    <% } else { %>
        <!-- Display Content -->
        <p style="font-size: smaller;"><%= blog.subtitle %></p>
        <p style="font-size: smaller; font-style: italic;"><%= 'Written by ' + blog.author %></p>
        <p><%= blog.body %></p>
        <!-- Edit Button -->
        <a href="/index/blogs/<%= blog._id %>/edit">Edit</a>
        <br>
        <br>
        <a href="/index/blogs/<%= blog._id %>/delete" onclick="return confirm('Are you sure you want to delete this blog?')">Delete this post</a>
        <br>
        <br>
        <a href="/index">Home</a>
    <% } %>

</body>
</html>
